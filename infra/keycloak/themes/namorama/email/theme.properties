# `parent=keycloak` et non `base` : le thème parent fournit les gabarits que
# nous ne surchargeons pas (mot de passe oublié par code, mails d'organisation,
# etc.). Avec `base`, tout template absent d'ici part au format brut, sans
# aucune mise en forme — c'était le cas jusqu'ici.
parent=keycloak
